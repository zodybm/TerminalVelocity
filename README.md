# TerminalVelocity
This computes the terminal velocity.


This assumes the formula v=sqrt((2*m*g)/(d*p*A))

v = terminal velocity
m = mass
g = force of gravity
d = air density
p = drag coefficient
A = area
